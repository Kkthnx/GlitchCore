# Normalize line endings to LF in the repo (stops the CRLF conversion warnings
# on Windows). Binary assets are left untouched.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ttf binary
*.otf binary
